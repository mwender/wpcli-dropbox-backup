#!/usr/bin/env bash

#Dropbox config file
DROPBOXCONFIG=.dropbox_uploader

#User's home directory
USERPATH=/home/dir/for/user

#define local path for backups
BACKUPPATH=/local/path/to/backups

#path to WordPress installations
SITESTORE=/path/to/WordPress/installations

#WordPress install sub-directory name (no slashes)
WEBROOT=public

#Days to retain
DAYSKEEP=3